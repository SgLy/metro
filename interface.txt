list_all_station()
	-> Return all stations' name available(With index).

struct Response {
	vector<pair<int, vector<string> > > path; vector<pair<line_index, vector<station_name> > >
	int money; -> money costed.
	int cost_time; -> time costed
	double distance. -> in km
	vector<int> time_between_station; -> time in minutes, cost between each station. n stations, (n-1) elements in this vector.
}

Response query_money(int start_station_index, int end_station_index)
Response query_distance(start_station_index, end_station_index)
Response query_time(start_station_index, end_station_index)

int query_station_index(const string &)
string query_station_name(const int &)
